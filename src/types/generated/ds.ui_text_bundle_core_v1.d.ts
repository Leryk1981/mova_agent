/* tslint:disable */
/* eslint-disable */
/**
 * This file was automatically generated by scripts/gen_types.ts.
 * Do not edit manually. Update JSON Schemas and rerun npm run gen:types.
 */

/**
 * Core schema for UI text bundles with strict separation between human-facing text, model instructions, and system/log text.
 */
export type DsUiTextBundleCoreV1 = DsMovaSchemaCoreV1 & {
  /**
   * Identifier of this text bundle within a template or application.
   */
  bundle_id: string;
  /**
   * Optional id of the context this bundle belongs to (e.g. envelope id, step id, screen id).
   */
  context_id?: string;
  /**
   * Logical role of this UI text in the flow.
   */
  role: 'question' | 'explanation' | 'hint' | 'error_message' | 'success_message' | 'section_title' | 'other';
  /**
   * Human-facing UI text that is shown directly to the user.
   */
  human_text: {
    /**
     * Channel id from global.text_channel_catalog_v1. Must be human_ui.
     */
    channel: 'human_ui';
    /**
     * IETF language tag for the human text, e.g. uk, de, en.
     */
    lang?: string;
    /**
     * Text shown to the human user. Must not contain direct instructions for an LLM.
     */
    text: string;
  };
  /**
   * Optional model-only instruction text that accompanies this UI element, never shown to the user.
   */
  model_text?: {
    /**
     * Channel id from global.text_channel_catalog_v1. Must be model_instruction.
     */
    channel: 'model_instruction';
    /**
     * IETF language tag for the model text, if applicable.
     */
    lang?: string;
    /**
     * Instruction or guidance for the model on how to interpret this UI element and related data.
     */
    text: string;
  };
  /**
   * Optional system/log text for diagnostics; never used as prompts or shown to users.
   */
  log_text?: {
    /**
     * Channel id from global.text_channel_catalog_v1. Must be system_log.
     */
    channel: 'system_log';
    /**
     * IETF language tag for the log text, if applicable.
     */
    lang?: string;
    /**
     * Technical log or diagnostic text associated with this UI context; not a prompt.
     */
    text: string;
  };
};

/**
 * Core form for MOVA data schemas (ds.*) instances: common service fields schema_id, schema_version, meta, ext.
 */
export interface DsMovaSchemaCoreV1 {
  /**
   * Canonical schema id for this data record, e.g. ds.smartlink_config_v1. Recommended but optional.
   */
  schema_id?: string;
  /**
   * Version of the schema used when this record was produced, e.g. 1 or 1.0.0.
   */
  schema_version?: string;
  /**
   * Human- and operator-oriented metadata: title, description, tags, notes. Must not contain execution instructions for LLMs.
   */
  meta?: {
    /**
     * Short human-readable title of this record.
     */
    title?: string;
    /**
     * Longer human-readable description or notes.
     */
    description?: string;
    /**
     * Optional list of tags for searching and grouping.
     */
    tags?: string[];
    /**
     * IETF language tag describing primary human language of title/description, e.g. uk, en, de.
     */
    lang?: string;
    [k: string]: unknown;
  };
  /**
   * Machine-oriented extensions: technical flags, computed hints, links to external systems. Must not contain LLM instructions.
   */
  ext?: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
}
