{
  "id": "external_call_webhook_v0",
  "version": "0.0.1",
  "claim": "Webhook external calls are policy-gated, noop-only, deterministic, and produce auditable evidence.",
  "surface": "ocp.external_call.webhook.v0",
  "scripts": {
    "pos": "npm run quality:external_call_webhook",
    "neg": "npm run quality:external_call_webhook:neg"
  },
  "fixtures": {
    "pos": [
      "packs/external_call_webhook_v0/fixtures/pos/webhook_noop_request.json"
    ],
    "neg": [
      "packs/external_call_webhook_v0/fixtures/neg/forbidden_target.json",
      "packs/external_call_webhook_v0/fixtures/neg/oversize_payload.json",
      "packs/external_call_webhook_v0/fixtures/neg/missing_target.json"
    ]
  },
  "evidence": {
    "pos_base": "artifacts/quality/external_call_webhook_v0/pos",
    "neg_base": "artifacts/quality/external_call_webhook_v0/neg",
    "expected_files": [
      "report.json"
    ]
  },
  "notes": [
    "v0 is noop-only; dry_run enforced; real endpoints forbidden"
  ]
}
