{
  "id": "ocp_delivery_v0",
  "version": "0.0.1",
  "claim": "Outbound delivery is policy-gated, deterministic, and produces auditable evidence artifacts.",
  "surface": "ocp.delivery.v0",
  "scripts": {
    "pos": "npm run quality:ocp_delivery",
    "neg": "npm run quality:ocp_delivery:neg"
  },
  "fixtures": {
    "pos": [
      "packs/ocp_delivery_v0/fixtures/pos/noop_delivery_request.json"
    ],
    "neg": [
      "packs/ocp_delivery_v0/fixtures/neg/forbidden_target.json",
      "packs/ocp_delivery_v0/fixtures/neg/oversize_payload.json",
      "packs/ocp_delivery_v0/fixtures/neg/missing_target.json",
      "packs/ocp_delivery_v0/fixtures/neg/secret_leak.json"
    ]
  },
  "evidence": {
    "pos_base": "artifacts/quality/ocp_delivery_v0/pos",
    "neg_base": "artifacts/quality/ocp_delivery_v0/neg",
    "expected_files": [
      "report.json"
    ]
  },
  "notes": [
    "v0 is noop-only; ALLOW_NOOP_ONLY must be true",
    "Real endpoints are forbidden; dry_run enforced"
  ]
}
